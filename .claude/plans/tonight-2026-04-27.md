# Tonight's session plan — 2026-04-27
**Total budget:** ~3 hours. Realistic: first two tasks; third is bonus.
**Discipline:** every fix must be runtime-validated by the user before
commit. No static-only PASSes for these. The bugs being fixed are the
direct result of accepting static-only PASSes on critical-path work.

---

## Task 1 — East-edge traversal (45-60 min)

**Bug:** Walking off NeighborhoodScene's east edge does nothing,
despite SCENE_ADJACENCY + attachEdgeSensor being wired correctly.
See `.claude/bugs/2026-04-27-quest-engine-and-traversal.md` (Bug 2).

**Files likely modified:**
- `src/renderer/game/systems/seamlessTraversal.js` (instrumentation
  + fix)
- Possibly `src/renderer/game/scenes/NeighborhoodScene.js` (if sensor
  positioning is the bug)
- Possibly `src/renderer/game/scenes/DryWashScene.js` (if
  applySeamlessEntry hook needs work)

**Process:**
1. With DevTools open, walk east. Confirm silence.
2. In Claude Code: ask for diagnostic logs at every link in the
   sensor pipeline (attachEdgeSensor zone creation, overlap callback
   fire, performSeamlessTransition entry, applySeamlessEntry entry).
   DO NOT request a fix yet — instrumentation only.
3. Run the game. Walk east. Console reveals the broken link.
4. Ask Claude Code for the targeted fix based on what the logs showed.
5. Verify: walk east, transition fires, land in DryWashScene at west
   spawn with preserved velocity. Walk back west; reverse transition
   fires.

**Runtime-validation criterion (user-confirmable):**
> Walking east from NeighborhoodScene transitions to DryWashScene at
> the west edge. Walking west from DryWashScene returns to
> NeighborhoodScene at the east edge. Both directions feel seamless
> (no spawn-point flash). Console is clean.

**On PASS:** commit with verdict `runtime-validated`.

---

## Task 2 — Quest engine fix + bridge_collapse end-to-end (60-75 min)

**Bug:** `questSystem.advanceQuest()` observe-branch is empty,
silently auto-advancing observe steps on dialog dismissal regardless
of `requiredObservation`. See bug log (Bug 1).

**Files likely modified:**
- `src/renderer/game/systems/questSystem.js` (the three-line gate
  + remove FIXME comment block)
- Optionally `src/renderer/game/data/quests.js` (if option C taken
  to mark 19 unwired quests as `prerequisite: 'TODO_emit_*'`)
- No emission-wiring this session (deferred to per-quest sessions)

**Process — three phases, stop after each for human eyes:**

### Phase 1 — Engine fix
Apply the three-line gate from the bug log. Remove the existing
FIXME comment block (the fix supersedes it). Confirm `npm run build`
passes.

### Phase 2 — Side-effects audit (Layer 2 decision)
Re-confirm the wired-vs-unwired list (should match the bug log: 2
wired, 19 unwired). Choose A/B/C from the bug log's options. Recommend
**C** — mark unwired quests' `prerequisite` field with
`TODO_emit_<observation>` placeholders so they hide from availability
until properly wired in future sessions.

### Phase 3 — Save-state non-regression check
Confirm: existing completed quests in the user's save remain
completed (the new gate is only forward-looking; it doesn't
retroactively un-complete past advances).

### Phase 4 — Replay bridge_collapse end-to-end
This is the actual end-to-end runtime validation:
1. Use `tools/inspect-save.js --reset-quest bridge_collapse` to
   produce a save with `bridge_collapse` removed from completedQuests.
2. Paste that back into the running app's localStorage via DevTools.
3. Reload, restart the bridge_collapse quest from the giver.
4. Walk through every step. At Mr. Chen's "go build the bridge"
   dialog, dismissing it must NOT auto-complete the quest — the quest
   must wait at `build_bridge` for the `bridge_built` observation.
5. Walk east (Task 1 must be complete) → DryWashScene → run
   construction → confirm `bridge_built` observation arrives →
   confirm quest advances to `complete_bridge` and only then
   completes.

**Runtime-validation criterion (user-confirmable):**
> bridge_collapse can be replayed start-to-finish on a reset save.
> The quest pauses at every observe step until the observation
> arrives. The actual build is required (not optional).
> One unrelated quest with an observe step (any of the 19) is
> visibly affected — either gates correctly or hides from the
> available-quests list (option C).

**On PASS:** commit the engine fix + (if option C) the prerequisite
markers, both with verdict `runtime-validated`. The 19 unwired-quest
emission wiring is a SEPARATE, per-quest project for future sessions
— bug log already lists every gap.

---

## Task 3 — Pause storm investigation (optional, 30-45 min)

**Bug:** `LayoutEditorOverlayScene` save handler triggers ~20
`Cannot pause non-running Scene <key>` warnings per save. See bug
log (Bug 3). Cosmetic; no functional break.

**Files likely modified:**
- `src/renderer/game/scenes/LayoutEditorOverlayScene.js`
- Possibly a shared `pauseAllScenes` / `restartActive` helper if one
  exists.

**Process:**
1. Open layout editor (F2), edit a position, save. Console fills.
2. Find the iteration that calls `pause()` across all registered
   scenes. Add diagnostic logs first to confirm which loop is the
   noisy one.
3. Filter the iteration by `scene.sys.isActive() ||
   scene.sys.isPaused()` before pausing — or pause only the
   relevant scene rather than broadcasting.

**Runtime-validation criterion (user-confirmable):**
> Open layout editor, edit a position, save. Console shows zero
> "Cannot pause non-running Scene" warnings. Repeat 3× in a session;
> no warning accumulation.

**On PASS:** commit with verdict `runtime-validated`. Tag as cosmetic
in the commit message so it's clear this is polish, not gameplay.

---

## What is explicitly NOT on tonight's list

- The 19 unwired-observation quests. Their per-quest emission wiring
  is a future project, scheduled per quest based on player priority.
- Any other pod from the swarm queue (resource-auditor, bundle-splitter,
  audio-* agents, etc.). Tonight is focused on the runtime bugs that
  the prior session's static-pass PASSes failed to catch.
- Any new pod design or sequencing.yaml additions. Tonight is fix
  work, not feature work.

## Discipline reminder

The Phaser DryWash pod (commits 96eceb7 / f647de8 / 04d3fda) was
closed as "end-to-end playable" without any runtime testing, and the
two bugs being fixed tonight surfaced within hours. Per the hardened
PASS-verdict rule (commit c745149), tonight's commits MUST be tagged
`runtime-validated` and the user must confirm each fix in a running
build before commit. No exceptions.
