{
  "agent": "gamecontainer-defer-boot",
  "ts": "2026-04-26T02:35:49Z",
  "status": "complete",
  "files_changed": ["src/renderer/game/GameContainer.jsx"],
  "notes": "All four edits landed cleanly in src/renderer/game/GameContainer.jsx. useState was already imported from 'react' (line 16), no new import needed.\n\n=== EDIT 1: Add containerReady state declaration ===\nBEFORE (lines 74-80):\nexport default function GameContainer() {\n  const containerRef = useRef(null);\n  const gameRef = useRef(null);\n  const prevCompletedRef = useRef(new Set());\n  const navigate = useNavigate();\n  const recordQuestComplete = useLearningStore((s) => s.recordQuestComplete);\n\nAFTER (lines 74-80):\nexport default function GameContainer() {\n  const containerRef = useRef(null);\n  const gameRef = useRef(null);\n  const [containerReady, setContainerReady] = useState(false);\n  const prevCompletedRef = useRef(new Set());\n  const navigate = useNavigate();\n  const recordQuestComplete = useLearningStore((s) => s.recordQuestComplete);\n\n=== EDIT 4: Reset gate on bootKey change ===\nInserted at lines 203-207 (immediately before the new dimension-poll useEffect):\nBEFORE (context — what was at line ~202):\n  const cancelNewGame = useCallback(() => {\n    setConfirmNewGame(false);\n  }, []);\n\n  // ------------------------------------------------------------------\n  // Boot Phaser (only when phase === 'playing')\n\nAFTER (lines 198-220):\n  const cancelNewGame = useCallback(() => {\n    setConfirmNewGame(false);\n  }, []);\n\n  // Reset the gate when bootKey changes (e.g., Reset Game) so the new\n  // Phaser instance also waits for valid container dimensions.\n  useEffect(() => {\n    setContainerReady(false);\n  }, [bootKey]);\n\n  // ------------------------------------------------------------------\n  // Gate: wait for the container to have real dimensions before\n\n=== EDIT 2: New dimension-poll useEffect ===\nInserted at lines 209-244, IMMEDIATELY BEFORE the existing boot useEffect:\nBEFORE (context — old line ~202-211):\n  // ------------------------------------------------------------------\n  // Boot Phaser (only when phase === 'playing')\n  // ------------------------------------------------------------------\n  useEffect(() => {\n    if (phase !== 'playing') return;\n    if (!containerRef.current) return;\n\n    // Guard: prevent Vite HMR from creating duplicate Phaser instances\n    if (gameRef.current) return;\n\nAFTER (lines 209-255):\n  // ------------------------------------------------------------------\n  // Gate: wait for the container to have real dimensions before\n  // booting Phaser. On cold start, AppLayout's 100dvh height +\n  // Suspense lazy-load mean clientWidth/Height return 0 right after\n  // mount. If we boot Phaser at 0×0, every scene's create() reads\n  // those zero dimensions from this.cameras.main and bakes them into\n  // node positions, parchment rectangles, etc. Even after the canvas\n  // later resizes (via the ResizeObserver below), the scene content\n  // stays clustered in the top-left because positions are already\n  // locked in. Solution: don't boot until the layout has settled.\n  // ------------------------------------------------------------------\n  useEffect(() => {\n    if (phase !== 'playing') return;\n    if (!containerRef.current) return;\n    if (containerReady) return;\n\n    const el = containerRef.current;\n\n    const check = () => {\n      if (el.clientWidth > 0 && el.clientHeight > 0) {\n        setContainerReady(true);\n        return true;\n      }\n      return false;\n    };\n\n    if (check()) return;\n\n    const observer = new ResizeObserver(() => {\n      if (check()) {\n        observer.disconnect();\n      }\n    });\n    observer.observe(el);\n    return () => observer.disconnect();\n  }, [phase, containerReady]);\n\n  // ------------------------------------------------------------------\n  // Boot Phaser (only when phase === 'playing')\n  // ------------------------------------------------------------------\n  useEffect(() => {\n    if (phase !== 'playing') return;\n    if (!containerReady) return;       // NEW: don't boot at 0×0\n    if (!containerRef.current) return;\n\n    // Guard: prevent Vite HMR from creating duplicate Phaser instances\n    if (gameRef.current) return;\n\n=== EDIT 3 (a + b): Boot useEffect guard + dependency array ===\nGuard added inline (see Edit 2 'AFTER' above — line 251 'if (!containerReady) return;').\n\nDependency array change at line 452:\nBEFORE:\n    return () => {\n      resizeObserver.disconnect();\n      game.destroy(true);\n      gameRef.current = null;\n    };\n    // eslint-disable-next-line react-hooks/exhaustive-deps\n  }, [phase, bootKey]);\n\nAFTER:\n    return () => {\n      resizeObserver.disconnect();\n      game.destroy(true);\n      gameRef.current = null;\n    };\n    // eslint-disable-next-line react-hooks/exhaustive-deps\n  }, [phase, bootKey, containerReady]);\n\n=== Verification ===\n- useState already imported (line 16): import { useEffect, useRef, useState, useCallback } from 'react';\n- containerReady state declared (line 77).\n- bootKey-reset useEffect at lines 203-207.\n- Dimension-poll useEffect at lines 209-244 immediately precedes boot useEffect.\n- Boot useEffect now has 'if (!containerReady) return;' as second guard (line 251).\n- Boot useEffect dependency array now includes containerReady (line 452).\n- Previous worker's ResizeObserver-for-refresh code is fully intact: new ResizeObserver at line 278, gameRef.current.scale.refresh() at line 280, observe at line 283, cleanup disconnect at line 447. Comment block (lines 259-265) referencing the ResizeObserver is unchanged.\n- 180-line boot useEffect body otherwise untouched."
}
