{
  "agent": "gamecontainer-resize-fix",
  "ts": "2026-04-26T02:28:52Z",
  "status": "complete",
  "files_changed": ["src/renderer/game/GameContainer.jsx"],
  "notes": "Two surgical edits applied to the boot useEffect in src/renderer/game/GameContainer.jsx. ResizeObserver is a renderer global; no new imports added. Nothing outside the two specified locations changed.\n\n=== EDIT 1 — top of boot useEffect (~line 212) ===\n\nBEFORE (5 lines context each side):\n----\n    if (!containerRef.current) return;\n\n    // Guard: prevent Vite HMR from creating duplicate Phaser instances\n    if (gameRef.current) return;\n\n    const el = containerRef.current;\n\n    // Wait until the container has real dimensions (layout may not be\n    // painted yet when lazy-loaded via Suspense).\n    const width = el.clientWidth || window.innerWidth;\n    const height = el.clientHeight || window.innerHeight;\n\n    const savedState = loadGame();\n    const startScene = savedState.player?.scene || 'ZuzuGarageScene';\n\n    const config = createGameConfig(el, width, height, startScene);\n    const game = new Phaser.Game(config);\n    gameRef.current = game;\n\n    // Seed registry with saved state\n    game.registry.set('gameState', savedState);\n    prevCompletedRef.current = new Set(savedState.completedQuests || []);\n----\n\nAFTER (5 lines context each side):\n----\n    if (!containerRef.current) return;\n\n    // Guard: prevent Vite HMR from creating duplicate Phaser instances\n    if (gameRef.current) return;\n\n    const el = containerRef.current;\n\n    // Initial measurement — may be 0×0 on cold start when AppLayout's\n    // 100dvh height hasn't resolved yet (lazy-loaded via Suspense). The\n    // ResizeObserver below catches that case by calling scale.refresh()\n    // as soon as the layout settles, AND on any subsequent parent resize\n    // (window chrome changes, devtools open/close, etc.). Phaser's\n    // Scale.RESIZE mode only tracks `window` resize events natively — it\n    // does NOT observe parent-element size changes.\n    const width = el.clientWidth || window.innerWidth;\n    const height = el.clientHeight || window.innerHeight;\n\n    const savedState = loadGame();\n    const startScene = savedState.player?.scene || 'ZuzuGarageScene';\n\n    const config = createGameConfig(el, width, height, startScene);\n    const game = new Phaser.Game(config);\n    gameRef.current = game;\n\n    // Track parent-element resizes so Phaser re-fits the canvas whenever\n    // layout shifts (cold-start race, window resize, devtools toggle).\n    const resizeObserver = new ResizeObserver(() => {\n      if (gameRef.current) {\n        gameRef.current.scale.refresh();\n      }\n    });\n    resizeObserver.observe(el);\n\n    // Seed registry with saved state\n    game.registry.set('gameState', savedState);\n    prevCompletedRef.current = new Set(savedState.completedQuests || []);\n----\n\n=== EDIT 2 — cleanup return at bottom of boot useEffect (~line 401) ===\n\nBEFORE (5 lines context each side):\n----\n    // If audio is already unlocked, inject it\n    if (isUnlocked && audio) {\n      game.registry.set('audioManager', audio);\n    }\n\n    // Cleanup on unmount\n    return () => {\n      game.destroy(true);\n      gameRef.current = null;\n    };\n    // eslint-disable-next-line react-hooks/exhaustive-deps\n  }, [phase, bootKey]);\n----\n\nAFTER (5 lines context each side):\n----\n    // If audio is already unlocked, inject it\n    if (isUnlocked && audio) {\n      game.registry.set('audioManager', audio);\n    }\n\n    // Cleanup on unmount\n    return () => {\n      resizeObserver.disconnect();\n      game.destroy(true);\n      gameRef.current = null;\n    };\n    // eslint-disable-next-line react-hooks/exhaustive-deps\n  }, [phase, bootKey]);\n----\n\nVerification: post-edit Read confirmed the new comment block sits at lines 214-220, the ResizeObserver block (const resizeObserver = new ResizeObserver(...)) sits at lines 231-238 immediately after `gameRef.current = game;`, and the cleanup return at lines 401-405 includes `resizeObserver.disconnect();` BEFORE `game.destroy(true);`. Vite HMR was not running; user will restart manually."
}
