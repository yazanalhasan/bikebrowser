{
  "agent": "neighborhoodscene-race-fix",
  "ts": "2026-04-25T12:00:00Z",
  "status": "complete",
  "files_changed": ["src/renderer/game/scenes/NeighborhoodScene.js"],
  "notes": "Fixed runtime crash 'TypeError: Cannot read properties of undefined (reading <npcId>)' at NeighborhoodScene.js:489 caused by advanceFromDialog() being invoked after scene teardown (scene-pause-race). Two surgical edits, no refactor, no other files touched.\n\n=== EDIT 1: Early-return guard at top of advanceFromDialog() ===\n\nBEFORE (lines 451-462):\n    if (!step) return;\n\n    this._emitEnrichedDialog(npcId, questId, step, state);\n  }\n\n  advanceFromDialog(choiceIndex) {\n    let state = this.registry.get('gameState');\n    const audioMgr = this.registry.get('audioManager');\n    const result = advanceQuest(state, choiceIndex);\n\n    if (!result.ok) {\n\nAFTER (lines 451-467):\n    if (!step) return;\n\n    this._emitEnrichedDialog(npcId, questId, step, state);\n  }\n\n  advanceFromDialog(choiceIndex) {\n    // Guard: this can be invoked from the React-side dialog UI after the scene\n    // has been paused/shut down (matches the \"Cannot pause non-running Scene\"\n    // race in the renderer log). Bail before touching registry/audio/refs.\n    if (!this._npcBikes || !this.scene?.isActive?.()) return;\n\n    let state = this.registry.get('gameState');\n    const audioMgr = this.registry.get('audioManager');\n    const result = advanceQuest(state, choiceIndex);\n\n    if (!result.ok) {\n\n=== EDIT 2: Optional-chain on _npcBikes access (~line 489 -> now 494) ===\n\nBEFORE (lines 488-497, pre-edit numbering):\n    } else {\n      audioMgr?.playSfx('ui_success');\n    }\n\n    NPC_PLACEMENTS.forEach((npcData) => {\n      if (state.completedQuests?.includes(npcData.questId) && this._npcBikes[npcData.id]) {\n        this._npcBikes[npcData.id].setText('\\uD83D\\uDEB2\\u2705');\n      }\n    });\n\n    const nextStep = getCurrentStep(state);\n\nAFTER (lines 489-498, post-edit numbering — line numbers shifted +5 from edit 1):\n    } else {\n      audioMgr?.playSfx('ui_success');\n    }\n\n    NPC_PLACEMENTS.forEach((npcData) => {\n      if (state.completedQuests?.includes(npcData.questId) && this._npcBikes?.[npcData.id]) {\n        this._npcBikes[npcData.id].setText('\\uD83D\\uDEB2\\u2705');\n      }\n    });\n\n    const nextStep = getCurrentStep(state);\n\nVerification: re-read file lines 450-505 after edits; both changes present, no other lines modified, _npcBikes init in create() (line 83) untouched, existing pause/re-enter race guards in working tree untouched."
}
