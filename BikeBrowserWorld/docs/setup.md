# BikeBrowserWorld Setup

Open the project with:

```text
C:\Godot\Godot_v4.6.2-stable_win64.exe
```

Then choose:

```text
C:\Users\yazan\bikebrowser\BikeBrowserWorld\project.godot
```

The main scene is:

```text
res://Regions/Boot/Boot.tscn
```

The prototype uses a separate save key:

```text
bikebrowser_godot_test_save
```

It must not write to the Phaser save key:

```text
bikebrowser_game_save
```
