# BikeBrowserWorld Tests

Early Godot verification is performed from the parent repo:

- React bridge schema: `tests/godot-bridge.test.mjs`
- React prototype smoke: `tests/e2e/godot-prototype.smoke.spec.js`
- Existing Phaser regression tests remain under `tests/e2e/`

Godot-side test harnesses can be added after the project loads cleanly in headless mode.
