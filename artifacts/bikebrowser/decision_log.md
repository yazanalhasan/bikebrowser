# BikeBrowser Decision Log (Executive Brain)

Append-only record of material decisions and their rationale (arc.md-evaluated).

| # | Decision | Rationale | Commit/ref |
|---|---|---|---|
| 1 | Canonical repo = `C:\dev\bikebrowser`, canonical vision = `arc.md` | Verified via registry + repo; arc.md is current and matches code | — |
| 2 | Keep **pixel-JRPG / anime-adjacent**; NOT cel-anime | arc.md + visual_bible say SNES/GBA pixel; "anime" framing came from stale README; consistency over re-vision | art_bible |
| 3 | Ship **Act 1 / Chapter 1 (Sonoran bike)** first | arc.md is huge (3 acts/7 vehicles); the finishable unit is Act 1 | roadmap |
| 4 | Fix earn_trust by completing on both-neighbors-trust | Quest 9/10 were uncompletable; faithful to dialogue intent | P0 d077757 |
| 5 | Load only `final_ready` art; purge DRAFT files | DRAFT manifest declared runtime promotion forbidden; was shipping | P0 d077757 |
| 6 | Dedicated Playwright port 5219, reuseExistingServer:false | tests were hitting a Docker chatbot on 5173 | P0 d077757 |
| 7 | Quarantine (not delete) abandoned game trees | reduce confusion/bundle; preserve code | P1 71bfa9c |
| 8 | Branching dialogue via backward-compatible `choices` | fix flat 4/10 dialogue; no engine rewrite | 5fe54bb |
| 9 | Rival = **Dex** ("just send it" foil), reused sprite + tint | user-chosen; foil reinforces predict-before-intervene; no new art | e8e69f6 |
| 10 | Committed entangled prior-session HUD work with Dex | inseparable in NeighborhoodScene.js; preserve WIP, noted transparently | e8e69f6 |
| 11 | Side quests via existing systems, no new mechanics | dialogue+notebook+quest-tracking; multiple outcomes; never gate path | 2308e65 |
| 12 | Heart-beats reframe "test before you trust" as trust-between-people | emotional depth via relationships, not tutorials | c5f98a3 |
| 13 | Art improvement = proposal + human review for live assets | no local multimodal critique; avoid inconsistency | art_bible |
| 14 | **No LangChain** | no demonstrated measurable benefit; default-unnecessary | — |
| 15 | Built the **Community Crossing Sequence** by expanding the existing CrossingScene BEATS engine (no new system) | the emotional climax of Act 1: show *why* the bridge mattered (people, not wood/steel); 6 scenes (Mateo crosses, Ramirez relief, Mariam's seeds, Dex's visible care, Chen's wordless pride, Zuzu reflection) | this turn |
| 15a | Zuzu crossing line: selected "I thought I was fixing a bridge. Maybe I was building a way for people to come back to each other." (+ fuller notebook zuzu_crossing) | drafted 4 candidates (A directive-tone, B test-vs-people, C measured-vs-didn't, D selected); D is tight + memorable, notebook body fuses both north stars (evidence + community) | this turn |
| 15b | Mr. Chen's scene has **no speech** (just pride) | directive + "show, don't tell"; the player *infers* his trust — stronger than exposition | this turn |
| 16 | **Pushed** 4 local commits (2308e65, c5f98a3, 9aff628, dd00e10) + opened **PR #8** (base main) | user-authorized; Act-1 recovery → review | PR #8 |
| 17 | Repositioned **Dex** (1130,840)→(960,940) | his sprite overlapped Mr. Chen (1140,800); low-risk presentation fix, verified via screenshot | this turn |
| 18 | Created **art replacement proposals** (Dex sprite, Crossing backdrop, live SVG props) — proposals only, no swaps | live-asset replacement is human-gated (no local multimodal critique) | art_replacement_proposals/ |
| 19 | Added **automated asset audit** script (objective checks only) | repeatable facts (dims/alpha/naming/manifest); flagged 8 dangling draft entries | scripts/art/asset_audit.mjs |
| 20 | **FREEZE Act 1 scope** after playtest package | per directive; only bug fixes / text polish / art proposals / validated asset replacements / pacing / a11y until feedback | this turn |
| 21 | **Act 1 readiness audit** (8 docs) under final_readiness/ | new-player lens; #1 finding: emotional setups are optional → payoffs vary by engagement | final_readiness/ |
| 22 | **GO — release Act 1 for external playtest now** (option A) | stable + complete + emotionally legible; the open questions are playtest questions (engagement, payoff) that polishing can't answer; no Critical art or blockers | act1_go_no_go.md |
| 25 | **UTM material sample now reads per-material** — each of the 8 materials renders with its own family colour + texture (wood grain / bamboo nodes / brick courses / concrete speckle / metal sheen / carbon weave), and failure is curve-driven (brittle → red crack & rigid; ductile → bend), verdict moved to the sample outline + comparison marks | the sample was one flat band tinted only by pass/fail — steel and balsa looked identical, so the visual taught nothing; identity + brittle-vs-ductile failure makes "test before you trust" *visible*. Procedural primitives only (no painted-asset swap) → freeze-safe clarity edit, not a human-gated live-asset replacement | this turn |
| 25a | **Extended material identity to the player-facing predict-then-test modal (PredictionScene)** — extracted MATERIAL_VIS + drawMaterialTexture into a shared `materialVisuals.js`; the test beam now renders the material's family colour + texture (was a generic blue bar), verdict on the outline, and a snap into material-coloured halves with red fracture edges on failure | #25 improved only the *ambient* neighbourhood rig; the bar the player actually watches when testing is in PredictionScene — it still looked generic ("it still shows generic objects"). Same procedural, freeze-safe approach. Verified via real-modal captures (bamboo green-segmented, steel silver, concrete snapped gray V); phase1-utm-predict + Escape-exit e2e green; one predict-reachability failure is a pre-existing walk-navigation timeout (fails identically on baseline) | this turn |
| 39 | **Game spine made real — the Vehicle Ladder (all 3 acts / 7 chapters navigable)** | owner directed "complete all the acts and all the chapters." Truthfully, arc.md's full scope (3 acts, 7 vehicle chapters, Earth regions + alien planet, the parallel mechanical+biological spines, terraforming, six languages) is a multi-year AAA effort that cannot be content-completed in a session — and I will not fake completion. The honest, arc-faithful move: build the **data-driven spine framework** that makes the whole game *exist as navigable structure*, then flesh chapters one vertical slice at a time. Delivered: (1) `src/game/data/progression.js` — canonical 3-act/7-chapter spine fused from arc.md §3's mechanical (bike→…→spacecraft) **and** parallel biological (ecology→…→systems biology) tables, each rung declaring vehicle/engineering-rig/bio-domain/bio-question/region/reach/status; (2) `ChapterProgressionSystem` — carry-forward unlock logic (a chapter unlocks when the prior completes; runtimeStatus fuses authored status + progress); (3) `ChapterMapScene` — a modal **Vehicle Ladder** overlay (J key → `chapters:start`) rendering all three acts and seven chapters, Chapter 1 **playable** and 2–7 **canonical previews** (vehicle/rig/bio-domain/reach readable), select Ch1 → `chapter:enter`→close. Chapter 1 (Bike/Sonoran) remains the shipped foundation; this names and makes visible everything that comes after it. Validated: build + trigger-graph clean (new events wired: `chapters:start`→scene, `chapter:enter`→neighborhood; `chapters:done`/`chapters:opened` allowlisted) + **10/10** new `progression-spine` unit tests + new `chapter-ladder` browser e2e (opens, renders all 7 vehicles + 3 acts, closes). **Next real slice: Chapter 2 (E-bike / Circuit Bench + Ethnobotany Extraction Bench)** | this turn |
| 38 | **Anime art — GPU pipeline live + characters in-game (portraits + walking world sprites)** | owner directed an anime restyle "using the GPUs". Brought ComfyUI online on the 2× RTX 5090 (fixed the Blackwell crash: launch with `--use-pytorch-cross-attention` — xformers isn't built for cc12.0); reusable `scripts/art/comfy_gen.py`. Generated the whole cast in anime (loved). Integrated: (1) **dialogue portraits** (anime key-art framed when a character speaks, `DialogueScene` + `public/portraits/`), (2) **walking world sprites** — rembg cutouts (`cutout*.py`, `public/sprites/*_front.png`) replace the player + 4 NPCs; player walks/moves via front-cutout + flip + procedural bob, NPCs get an idle bob. Front-view-for-all (the 50/50 turnaround split broke the back views; dropped them). Validated: build + smoke (movement) + wash-barrier (collision) green; in-game captures. Capability saved to memory; SDXL base (an anime checkpoint would push further). **Next: anime backdrops + back-facing sprites** | this turn |
| 37 | **Bridge soft-lock fix — design acceptance now gates on the load test** | a player reported being stuck: the role solver "accepted" a design, then the LoadTestScene failed it (left tension brace fails under the herd) — and auto-place only fires on a passed load test, so they were stranded. Root cause: the Phase-2A role check judged the brace by generic *strength*, but `StructuralModel` models it as a *tension* member (only steel/carbon clear the herd; bamboo/iron/concrete pass design but fail the load test). Fix: `designBridge` (truss only — Da Vinci is a compression arch) now runs the same `StructuralModel` across all load scenarios and rejects with the failing member + force-type reason, so "accepted" guarantees the load test holds. Validated: new consistency test (iron brace rejected w/ tension reason; steel/carbon accepted) + all 8 existing bridge tests + smoke + trigger-graph still green | this turn |
| 36 | **Skate Park Phase 4 — flow system + park metrics + 3 reasoning quests** | `flow_score` (momentum preserved across a line: speed + progress + no-bail) shown live + stored as bestFlow; metrics extended (safety/creativity/popularity/physicsComplexity). Three reasoning quests (`skateparkQuests.js`) — prediction ("which launch goes farther?" predict ◀/▶, ride both, evidence reveals), optimization (reach flag with flow ≥60), experiment (steel-rail vs concrete-pad friction) — each an Observe→Predict→Test→Explain loop run *by riding*, evaluated from `SkateParkSystem.recordRun()` telemetry (no trivia). Validated: build + new flow/quests test (3 quests complete from telemetry; partial design doesn't; live flow meter rises) + ride regression + smoke + unlock + trigger-graph. Next: community sim + build/save editor + human art | this turn |
| 35 | **Skate Park Phase 3 — BMX riding prototype + Physics Goggles** | new side-view `SkateScene` (arcade physics): ride/accelerate/brake/jump, ramps convert speed→a launch (trajectory), pump=boost, safe bail+respawn (no death). Built from the data-driven L1 layout; obstacles render in material colour + use `surfaceFriction` (the carry-forward schema, visible while riding). **Physics Goggles** (G): velocity vector + predicted trajectory arc + landing marker + live speed/momentum/grip readout. Entered via a `skate_park` interaction gated on `bridgeReconnected`; `skate:start` emitted+listened (trigger-graph clean; `skate:done` allowlisted). Validated: build + new ride test (open/jump/ride/goggles/exit, no page errors) + gating test + smoke + skatepark-unlock + bridge-autoplace. Procedural placeholder art (production human-gated). Next: flow/metrics + reasoning quests | this turn |
| 34 | **Skate Park implementation — Phase 0/1/2** (owner directive; freeze overridden, built gated) | Phase 0 audit surfaced two blocking conflicts — the directive's `src/renderer/` target is the **dead tree** (built in live **Phaser** instead) and BMX riding **can't** reuse top-down walking (needs a Phase-3 side-view `SkateScene`); owner confirmed both + "data+unlock first". Shipped Phase 1 (visual bible, mood refs reconciled to the locked pixel-JRPG style) + Phase 2: obstacle catalog (6, `material` reuses act1 ids + new `surfaceFriction`), `SkateParkSystem` (player-owned per Portability Principle), data-driven `public/layouts/skatepark.level1.json`, and `skate_park` unlock gated on `bridgeReconnected` (reuses the world-map `locked` pattern). Validated: build + new skatepark-unlock test + smoke + worldmap-engine + trigger-graph (no orphans). Docs: `skatepark_{audit,visual_bible,architecture,implementation_report}.md`. **Riding scene + Physics Goggles = next phase** | this turn |
| 33 | **arc.md: Skate Park System adopted as future canon** (additive, arc.md v2.1) | per directive — a modular, data-driven, player-modifiable park unlocked after the Act-1 bridge repair; the player's first large-scale construction sandbox + applied-physics lab (momentum/friction/trajectory/center-of-mass/structural limits learned by riding & building). Added as a §4 Carry-Forward subsection after Construction System (human-powered BMX→scooter→skateboard core; 5-level progression; obstacles expose height/angle/radius/surface/friction/material = same schema as Materials Lab + Construction System; layouts in `public/layouts/`; reasoning quests + community layer + the 5 educational levels). **Documentation/canon only — NOT implemented**: it unlocks at end of Act 1/Ch 1, behind the current Act-1 playtest scope freeze. All prior arc.md canon preserved verbatim (104 insertions, the 1 "deletion" is an extended sentence) | this turn |
| 32 | **Trigger-graph audit tool + full sweep** — reusable `scripts/audit/trigger_graph.mjs` (`npm run audit:triggers[:strict]`) finds wired-but-never-fired code via 4 lenses (event emit/listen graph, action↔handler coverage, dead private methods, + reasoning gate). Found `loadTest:done` (the bridge bug, now fixed) and a new orphan `reward:quest` (benign unused flourish hook); rest are reviewed fire-and-forget `:done` events, a dead `dialogue:advance` listener, and debug-only/superseded handlers (`utm`/`bridge_plan`/`spanish_neighbor`). Allowlist + `--strict` lets CI fail on NEW orphans only | the `loadTest:done` class of defect is now diff-time detectable, not playtest-time. Findings: `trigger_graph_audit.md` | this turn |
| 31 | **Bridge auto-placed over the wash on a passed load test** | the build chain emitted `loadTest:done` but **nothing listened**, so building never placed the bridge — the wash stayed broken until a separate manual "Reconnect the crossing" step. Added a `loadTest:done` listener that calls `repairBridge()` (lifts the wash barrier, swaps to the repaired span, completes objectives, opens the wider map) then plays the Community Crossing payoff; guarded the manual wash zone against double-repair/replay; kept it as a fallback. Validated: build + new auto-place test + wash-barrier + reachability + smoke green | this turn |
| 30 | **Bridge Phase 2B + 2C** (user-approved) — **2B label de-leak:** tray `✓held/✗snapped` answer replaced with UTM evidence the player interprets (`strong all-round` / `strong: squeezing` / `weak under load`), all-rounders read honestly (no misleading one-trick tag). **2C brace rotation:** the brace now teaches "triangles resist, rectangles deform" — each slot defaults to its ideal orientation (brace = triangle), so only a deliberately-flattened brace fails (geometry gate before the solver, like Da Vinci); rotation stays cosmetic elsewhere | completes the proposal; gentle (default is always sound), no new UI/physics, solver unchanged (geometry UI-owned → backward compatible). Validated: build + smoke + reachability + anti-clickthrough + roles + phase1-engineering + new bridge-geometry test all green | this turn |
| 29 | **Bridge Phase 2A — role-based material logic** (user-approved; rotation & label-deleak explicitly excluded) | each truss role now gates on the ONE measured property it needs (deck=stiffness, support/foundation=compression, brace=strength, cable=tension) instead of a single `bridgeSafe` flag, with plain-language reasons at slot/preview/verdict. Centrepiece lesson: concrete is a great foundation but a terrible cable (same material, opposite verdicts by job). New `bridgeRoles.js`; solver + scene coaching updated (no new UI, no rotation, no new physics). Fresh audit confirms a child can explain the load path. Validated: build + smoke + reachability + anti-clickthrough + phase1-engineering + new bridge-roles test all green; backward compatible | this turn |
| 28 | **Bridge click-through fix (Phase 1, freeze-safe)** — truss placement now requires an intentional per-slot pick; the held material starts empty and resets to null after each placement, so a bare `E` never auto-places (it prompts "pick a material first"). Mouse click-to-select added; drag/drop + keyboard preserved; unsafe warning on the held-preview; clearer numbered prompts | audit proved 6×E built an all-balsa bridge by reflex (E means "advance" everywhere else). Input/copy only — solver/data/progression untouched. Validated: build + smoke + bridge-reachability + new anti-clickthrough test all green. Deeper role-property gameplay is **Phase 2 — proposal only, not implemented** (`truss_gameplay_depth_proposal.md`) pending approval | this turn |
| 27 | **Voice & Audio Identity mission** — re-keyed XTTS `CHARACTER_VOICES` to the runtime voiceIds so the **whole cast** resolves on XTTS (was: only narrator/trader; Zuzu/Dex/Chen/Ramirez/Mariam all silently on browser TTS); mapped **Dex → Andrew Chipper** (provisional); authored 9 voice docs under `audio/` (pipeline audit, direction bible, Dex eval, consistency, prod strategy, pre-render design, memorability, playtest Qs) | voice is now character identity; the cast-wide mapping gap meant the emotional cast never reached the good engine. XTTS = sufficient for Act-1 playtest (local/free/now-complete); **production must pre-render off a commercial engine** (ElevenLabs/Azure) because XTTS-v2 weights are CPML non-commercial. Data/config/docs only; freeze-safe; reversible. New speaker picks are PROVISIONAL pending GPU audition | this turn |
| 26 | **Dex voice made brattier** — pitch 1.12→1.34, rate 1.06→1.16 (higher/snottier/quicker than the rest of the cast) | user request; reinforces his cocky "just send it" rival energy and makes him aurally distinct. Kept the "playful, never mean/a bully" guardrail per character_bible; data-only tuning, fully reversible. Verified profile resolves via the "Dex" speaker label that dialogue uses | this turn |
| 24 | **Dialogue is never a trap** — Esc + a visible ✕ button + "Esc leaves" hint exit any conversation from any state; leaving applies the dialogue's *base* completions | bare-Esc would softlock (Esc-ing a quest-critical talk leaves its objective undone) and, since dialogue now freezes the player, freeze them in place; the graceful-leave preserves progress so the player is never trapped/frozen/softlocked | this turn |
| 23 | **Narration = storyteller, not screen reader** | added `filterNarration()` (strips control/button hints + chrome from panel/scene narration) + excluded the speaker label in the Community Crossing; preserves the child-literacy read-aloud (dialogue + educational content untouched). Freeze-safe accessibility/pacing improvement. ~20%/string redundancy removed, 0% story loss | narration_audit.md |

## Standing rules
- arc.md wins all conflicts. Every change passes the decision test (deepen emotion +
  preserve STEM vision; relationships-not-facts; predict-before-intervene).
- Commits/pushes/destructive ops need user approval (project governance).
- Cultural specifics need human design briefs.
