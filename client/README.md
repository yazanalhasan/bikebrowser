# Client

HTTP API client used by web/mobile UI.

Configuration:
- `VITE_API_BASE_URL`
- runtime override via localStorage key `bikebrowser_api_base`

Default base URL pattern:
- `http://<current-hostname>:3001`
