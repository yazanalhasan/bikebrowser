# Final Visual Excluded Worktree

Known exclusions from the scoped Act 1 visual-language commit:

- Unrelated Godot prototype files unless explicitly reviewed.
- `generated_assets/meshy/**` and `generated_assets/hunyuan3d/**` reference outputs.
- Rejected concept batches.
- Historical `project_audit/**` reports unrelated to this pass.
- Broad `playtest_captures/**` folders not regenerated for this pass.
- Existing dirty files outside the `/game-rebuild` Act 1 runtime, tests, captures, and docs.
- OpenClaw workspace changes, because OpenClaw was inspected but not used as a real executor.

