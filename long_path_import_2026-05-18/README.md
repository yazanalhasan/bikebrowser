# Long-Path Import 2026-05-18

This folder preserves selected operational artifacts copied from the older long-path clone:
- source clone: `C:\Users\admin\Documents\New project 3\bikebrowser`
- destination clone: `C:\dev\bikebrowser`

These files were copied into their own folder so they remain available for reference without being mixed into the active canonical repo surfaces.

Important:
- some documents here are historical and partially outdated
- in particular, early environment docs that recommend Node 20 should not override the later OpenClaw runtime stabilization work establishing Node 22 as the canonical supported runtime
- treat these files as imported context and helper tooling, not as the current source of truth unless revalidated
