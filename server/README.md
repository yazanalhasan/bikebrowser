# Server

Embedded HTTP API layer for BikeBrowser.

Key endpoints:
- POST /search
- POST /rank
- GET /projects
- POST /projects
- GET /notes
- POST /notes

The server binds to 0.0.0.0 on port 3001 and is started by Electron main process.
